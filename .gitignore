# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# lokale Testläufe gegen andere Umgebungen (siehe scripts/test-generate.mjs, TEST_OUT_SUFFIX)
test-data/output-*prod*.pdf

# Projekt-Handover mit Server-Interna: bleibt lokal, das Repo ist öffentlich
/PROJEKT-UEBERGABE.md

# Server-Setup mit Hostnamen/IPs: bleibt lokal, das Repo ist öffentlich
/deploy/

# Auf dem Server zeigt HOME auf das App-Verzeichnis (systemd-Unit setzt
# ProtectHome=true, /home ist dort nicht beschreibbar). npm und Chromium legen
# ihre Caches deshalb hier ab - sie gehoeren nicht ins Repo.
/.cache/
/.config/
/.local/
/.npm/
